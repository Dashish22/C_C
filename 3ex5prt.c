#include <stdio.h>

int main(void)
{
	int n, triangularNumber;
	
	n = 5;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 10;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

             n = 15;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 20;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);
           
            n = 25;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 30;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

             n = 35;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 40;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 45;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);

            n = 50;
	triangularNumber = n * (n + 1) / 2;
printf("triangularNumber is %i\n",triangularNumber);
	
	return 0;
}
